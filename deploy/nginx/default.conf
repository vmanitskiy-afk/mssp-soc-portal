upstream backend {
    server backend:8000;
}

upstream frontend {
    server frontend:80;
}

# Rate limiting zones
limit_req_zone $binary_remote_addr zone=api:10m rate=60r/m;
limit_req_zone $binary_remote_addr zone=login:10m rate=5r/m;

server {
    listen 80;
    server_name _;

    include /etc/nginx/snippets/security.conf;

    # For future Let's Encrypt validation
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # API — proxy to backend
    location /api/ {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        limit_req zone=api burst=30 nodelay;

        # Timeouts for long RuSIEM calls
        proxy_read_timeout 30s;
        proxy_connect_timeout 10s;

        # Max request body (for comments, etc.)
        client_max_body_size 10M;
    }

    # Stricter rate limit on login
    location /api/auth/login {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        limit_req zone=login burst=3 nodelay;
    }

    # Health check — no rate limit
    location /health {
        proxy_pass http://backend;
    }

    # Frontend — SPA
    location / {
        proxy_pass http://frontend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

# ── Uncomment when domain + SSL is ready ──────────────────────
# server {
#     listen 80;
#     server_name portal.yourcompany.ru;
#     return 301 https://$host$request_uri;
# }
#
# server {
#     listen 443 ssl http2;
#     server_name portal.yourcompany.ru;
#
#     ssl_certificate /etc/letsencrypt/live/portal.yourcompany.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/portal.yourcompany.ru/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     include /etc/nginx/snippets/security.conf;
#
#     location /api/ { ... same as above ... }
#     location / { ... same as above ... }
# }
